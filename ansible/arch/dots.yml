# make sure to have / at the end of src to only copy contents from "within" it
dev_dots:
  - src: "../../config/nvim/"
    dest: ".config/nvim/"
  - src: "../../config/kitty/"
    dest: ".config/kitty/"

hyprland_dots:
  - src: "../../config/hypr/"
    dest: ".config/hypr/"
  - src: "../../asset/wallpapers/"
    dest: "Pictures/Wallpapers"